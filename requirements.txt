# PAF-Model — local worker (scrapers + public.py)
# Install Playwright browsers after pip install:  playwright install chromium
#
# Run one worker process per backend:
#   python public.py --backend deepseek --vps ws://VPS_IP:PORT/ws/worker --workers 2
#   python public.py --backend qwen     --vps ws://VPS_IP:PORT/ws/worker --workers 2

playwright>=1.44.0
websockets>=12.0
python-dotenv>=1.0.1
tiktoken>=0.5.0          # optional but recommended (accurate token usage)
